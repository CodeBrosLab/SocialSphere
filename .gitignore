# Main Repository .gitignore

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# Environment files (if needed at root level)
.env.local
.env.development.local
.env.test.local
.env.production.local

# Temporary files
*.tmp
*.temp

# Build artifacts at root level (if any)
dist/
build/

# Node modules (if package.json is added at root level)
node_modules/

# Maven target (if pom.xml is added at root level)
target/

# Note: Submodule-specific ignores are handled in their respective .gitignore files
# This file only covers the main repository level